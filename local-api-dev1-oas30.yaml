openapi: "3.0.1"
info:
  title: "localfavs"
  version: "2023-06-30T03:21:52Z"
servers:
- url: "https://ny1czwx1si.execute-api.eu-west-2.amazonaws.com/{basePath}"
  variables:
    basePath:
      default: "dev1"
paths:
  /locals:
    get:
      responses:
        "200":
          description: "200 response"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Empty"
    post:
      responses:
        "200":
          description: "200 response"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Empty"
    delete:
      responses:
        "200":
          description: "200 response"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Empty"
components:
  schemas:
    Empty:
      title: "Empty Schema"
      type: "object"
