openapi: "3.0.1"
info:
  title: "events_auth"
  version: "2023-07-01T11:34:10Z"
servers:
- url: "https://akxqcs6ax1.execute-api.eu-west-2.amazonaws.com/{basePath}"
  variables:
    basePath:
      default: "dev"
paths:
  /auth:
    get:
      responses:
        "200":
          description: "200 response"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Empty"
    post:
      responses:
        "200":
          description: "200 response"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Empty"
components:
  schemas:
    Empty:
      title: "Empty Schema"
      type: "object"
